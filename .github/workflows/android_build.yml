# 工作流名称
name: android_build

# 触发条件
on:
  # 当推送代码到 master 或 beta 分支时触发
  push:
    branches: [ "master", "beta" ]
    # 忽略 Markdown 文件和 docs 目录的变更（避免不必要的构建）
    paths-ignore:
      - '**.md'
      - 'docs/**'
  
  # 当向 master 或 beta 分支发起 Pull Request 时触发（用于 CI 检查）
  pull_request:
    branches: [ "master", "beta" ]
  
  # 支持手动触发工作流（用于特殊情况下的强制发布）
  workflow_dispatch:
    inputs:
      release_notes:
        description: '发布说明（可选，手动触发时可填写变更内容）'
        required: false
        type: string

# 任务定义
jobs:
  # 第一阶段：构建与测试
  build:
    name: Build & Test                # 任务显示名称
    runs-on: ubuntu-latest            # 运行环境：最新 Ubuntu
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 配置 Java 环境（Flutter Android 构建必需）
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'     # Eclipse Temurin JDK
          java-version: '17'

      - name: 配置 Flutter 环境
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'           # 使用稳定通道
          cache: true                 # 启用缓存，加速后续构建

      - name: 安装项目依赖
        run: flutter pub get

      - name: 代码静态分析
        run: flutter analyze || true  # 分析错误不影响后续构建，仅警告

      - name: 构建安装包
        run: |
          # 根据当前分支选择构建类型
          if [[ "${{ github.ref_name }}" == "beta" ]]; then
            echo "正在构建 Beta 版本 APK..."
            flutter build apk --flavor beta --release
          else
            echo "正在构建正式版本 APK 和 AAB..."
            flutter build apk --flavor prod --release
            flutter build appbundle --flavor prod --release
          fi

      - name: 上传所有 APK 文件到 Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: apks                    # Artifact 名称，便于后续下载
          path: |
            build/app/outputs/flutter-apk/*.apk

      - name: 上传 AAB 文件（仅正式版需要）
        if: github.ref_name != 'beta'
        uses: actions/upload-artifact@v4
        with:
          name: aab
          path: build/app/outputs/bundle/prodRelease/app-prod-release.aab

  # 第二阶段：创建 GitHub Release
  release:
    name: Create Release               # 任务显示名称
    needs: build                       # 依赖 build 任务完成（确保产物已生成）
    runs-on: ubuntu-latest
    # 触发条件：代码推送 或 手动触发
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    permissions:
      contents: write                  # 需要写入权限才能创建 Release 和 Tag
    
    steps:
      - name: 检出代码（用于读取 pubspec.yaml）
        uses: actions/checkout@v4

      - name: 获取当前版本号
        id: version
        run: |
          # 从 pubspec.yaml 中提取完整版本（可能包含 +build）
          FULL_VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: //' | xargs)
          # 去掉 +build 及其后面的内容，只保留 x.y.z 部分（更美观）
          CLEAN_VERSION=$(echo $FULL_VERSION | sed 's/+.*//')
          # 输出供后续步骤使用
          echo "full_version=$FULL_VERSION" >> $GITHUB_OUTPUT
          echo "clean_version=$CLEAN_VERSION" >> $GITHUB_OUTPUT
          echo "检测到版本：$CLEAN_VERSION（原始：$FULL_VERSION）"

      - name: 下载构建产物
        uses: actions/download-artifact@v4
        with:
          path: artifacts                # 下载到 artifacts 目录

      - name: 创建正式版 Release（master 分支）
        if: github.ref_name == 'master'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.clean_version }}
          name: Release v${{ steps.version.outputs.clean_version }}
          body: |
            ## 🚀 正式版发布 v${{ steps.version.outputs.clean_version }}
            
            ${{ inputs.release_notes }}
            
            来自 master 分支的自动构建。
            
            ### 📦 下载
            - 正式版 APK
            - 正式版 AAB（用于上传 Google Play）
          files: |
            artifacts/apks/app-prod-release.apk
            artifacts/aab/app-prod-release.aab
          draft: false
          prerelease: false
          fail_on_unmatched_files: false    # 文件未匹配时不报错

      - name: 创建 Beta 版 Release（beta 分支）
        if: github.ref_name == 'beta'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.clean_version }}-beta
          name: Beta v${{ steps.version.outputs.clean_version }}
          body: |
            ## 🧪 测试版发布 v${{ steps.version.outputs.clean_version }}
            
            ${{ inputs.release_notes }}
            
            来自 beta 分支的自动构建。
            
            ### 📦 下载
            - Beta 版 APK
          files: |
            artifacts/apks/app-beta-release.apk
          draft: false
          prerelease: true                  # 标记为预发布版
          fail_on_unmatched_files: false