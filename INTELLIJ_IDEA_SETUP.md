# IntelliJ IDEA 配置说明

## 🎯 问题解决
你的超星网盘项目使用了 Flutter Product Flavors，现在已为 IntelliJ IDEA 配置了正确的运行配置！

## 🚀 如何使用

### 方法 1: 使用运行配置 (推荐)
1. 在 IntelliJ IDEA 右上角，点击运行按钮旁边的下拉菜单
2. 选择以下配置之一：
   - **`Chaoxing Disk - Beta Debug`** - 开发测试版本 ⭐ (日常开发用)
   - **`Chaoxing Disk - Production Debug`** - 生产测试版本
   - **`Chaoxing Disk - Production Release`** - 发布版本

### 方法 2: 手动创建运行配置
如果上面的配置没有显示，你可以手动创建：

1. 点击 `Run` -> `Edit Configurations...`
2. 点击 `+` -> `Flutter`
3. 配置如下：
   - **Name**: `Chaoxing Disk - Beta Debug`
   - **Flutter executable path**: 选择你的 Flutter SDK 路径
   - **Dart file**: `lib/main.dart`
   - **Additional arguments**: `--flavor beta`
   - **Build flavor**: `beta`

## ⚠️ 重要提醒

### ❌ 错误的做法 (会导致构建失败)
- 直接点击绿色的运行按钮 ▶️ (没有选择配置)
- 使用 `flutter run` 命令
- 使用 `flutter build apk` 命令

### ✅ 正确的做法
- **必须从运行配置下拉菜单中选择配置**
- 开发时选择 `Beta Debug`
- 发布时选择 `Production Release`

## 📱 当前运行状态
你的应用目前正在模拟器上运行：
- **版本**: Beta (测试版)
- **包名**: `textline.chaoxingrc.beta`
- **应用名**: 超星网盘(测试)

## 🔧 快捷操作
- **Shift + F10**: 运行当前选中的配置
- **Shift + F9**: 调试当前选中的配置
- **Ctrl + Shift + F10**: 运行当前文件

## 📁 配置文件位置
- 运行配置: `.idea/runConfigurations/`
- 主要配置文件:
  - `beta_debug.xml` - Beta 调试配置
  - `production_debug.xml` - 生产调试配置
  - `production_release.xml` - 生产发布配置

## 🛠 故障排除

### 如果配置不显示：
1. 关闭 IntelliJ IDEA
2. 重新打开项目
3. 等待索引完成
4. 检查运行配置下拉菜单

### 如果仍然失败：
1. 确保 Flutter 插件已安装
2. 确保 Flutter SDK 路径正确设置
3. 尝试重启 IntelliJ IDEA

---

现在你可以直接在 IntelliJ IDEA 中使用配置好的运行选项，不再需要手动输入命令了！🎉